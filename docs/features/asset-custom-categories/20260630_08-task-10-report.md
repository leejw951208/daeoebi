# Task 10 Report: asset-compute — byCategory 를 categoryId 기준으로

## Summary

`ComputedExpense.category: string` 을 `categoryId: string | null` 로 교체하고, `byCategory` 를 categoryId 기준 합산 + resolveCategory 조인 + 미분류 버킷 방식으로 재작성했다. `CategoryBreakdown` 에 `name: string` 필드 추가.

## TDD Evidence

### RED (테스트 먼저 수정, 실패 확인)

```
$ pnpm --filter web test asset-compute

FAIL node app/(vault)/asset/_lib/asset-compute.spec.ts
  ● asset-compute › byCategory 는 categoryId 로 합산하고 이름·색을 조인한다
    TypeError: Cannot read properties of undefined (reading 'map')
      at byCategory (asset-compute.ts:35:23)

  ● asset-compute › byCategory 는 categoryId=null 을 미분류로 묶는다
    TypeError: Cannot read properties of undefined (reading 'map')
      at byCategory (asset-compute.ts:35:23)

Tests: 2 failed, 4 passed, 6 total
```

### GREEN (구현 후 통과 확인)

```
$ pnpm --filter web test asset-compute

PASS node app/(vault)/asset/_lib/asset-compute.spec.ts
  asset-compute
    ✓ totalSpent 는 금액 합 (1 ms)
    ✓ byCategory 는 categoryId 로 합산하고 이름·색을 조인한다
    ✓ byCategory 는 categoryId=null 을 미분류로 묶는다
    ✓ byDay 는 일자별 합계 (1 ms)
    ✓ remaining·spentPct
    ✓ totalIncome 은 수입 금액을 합산한다

Tests: 6 passed, 6 total
Time: 0.179 s
```

## Files Changed

- `apps/web/app/(vault)/asset/_lib/asset-compute.ts`
  - import: `CATEGORIES`, `categoryColor` 제거 → `AssetCategory` (type) + `resolveCategory` 추가
  - `ComputedExpense.category: string` → `categoryId: string | null`
  - `CategoryBreakdown` 에 `name: string` 추가
  - `byCategory(items, categories)` 재작성: categoryId 기준 합산, resolveCategory 조인, null → "uncategorized" 버킷, amount>0 필터, 내림차순 정렬
- `apps/web/app/(vault)/asset/_lib/asset-compute.spec.ts`
  - `CATS` 픽스처 상수 추가
  - `exp()` 헬퍼: `category: "식비"` → `categoryId: null`
  - 기존 byCategory 두 테스트 → 새 테스트 두 건 (조인 검증 + 미분류 검증)

## Self-Review

- `totalSpent`, `remaining`, `spentPct`, `byDay`, `totalIncome` 등 나머지 함수는 변경 없음.
- `UNCATEGORIZED_KEY = "uncategorized"` 를 모듈 수준 상수로 선언해 매직 스트링 제거.
- amount>0 필터를 entries() 단계에서 처리해 불필요한 Set 순회 제거.
- 테스트 6개 전체 통과, 기존 byDay/totalSpent/remaining/spentPct/totalIncome 회귀 없음.
