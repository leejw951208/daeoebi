# Task 12 Report: ExpenseForm 카테고리 선택 동적 목록·categoryId 전환

## Files Changed

### 1. `apps/web/app/(vault)/asset/_components/ExpenseForm.tsx`
- Removed imports: `CATEGORIES`, `categoryColor` (no longer exported from asset-categories)
- Added import: `type AssetCategory` from `@/lib/vault-client`
- Added `categoryId: string | null` to `ExpenseFormInitial` interface
- Added `categories: AssetCategory[]` to `Props` interface
- Replaced `category: string` state with `categoryId: string | null` (initialized `initial?.categoryId ?? categories[0]?.id ?? null`)
- Updated `handleSave`: payload is now `{ item, amount }` (no `category`); all three branches (`createExpense`, `updateExpense`, `createRecurring`) now pass `categoryId: categoryId ?? undefined`
- Replaced `CATEGORIES.map` chip rendering with `categories.map((c) => ...)`: `active = c.id === categoryId`, color dot `background: c.color`, label `c.name`, click `setCategoryId(c.id)`

### 2. `apps/web/app/(vault)/asset/new/page.tsx`
- Added `useEffect`, `useState` imports
- Added `listAssetCategories, type AssetCategory` import from `@/lib/vault-client`
- Added `categories` state (loaded via `listAssetCategories()` on mount)
- Passed `categories={categories}` to `<ExpenseForm>`

### 3. `apps/web/app/(vault)/asset/[id]/page.tsx`
- Added `listAssetCategories, type AssetCategory` imports
- Added `categories: AssetCategory[]` state (loaded via `listAssetCategories()` in separate useEffect)
- Added `categoryId: view.categoryId` to `ExpenseFormInitial` when constructing initial state
- Passed `categories={categories}` to `<ExpenseForm>`

## tsc Error Confinement

Remaining errors after this task (all in Task 13 domain):

```
app/(vault)/asset/_components/dashboard/AssetDashboard.tsx(71,48): error TS2322 — 'categories' prop not yet on CategoryBreakdown
app/(vault)/asset/_components/dashboard/AssetDashboard.tsx(84,21): error TS2322 — 'categories' prop not yet on DayDetail
app/(vault)/asset/_components/dashboard/DayDetail.tsx(4,10): error TS2305 — no exported member 'categoryColor'
app/(vault)/asset/_components/dashboard/DayDetail.tsx(63,66): error TS2551 — Property 'category' does not exist on ComputedExpense
app/(vault)/asset/_components/dashboard/DayDetail.tsx(65,32): error TS2551 — Property 'category' does not exist on ComputedExpense
app/(vault)/asset/_components/dashboard/DayDetail.tsx(76,46): error TS2551 — Property 'category' does not exist on ComputedExpense
app/(vault)/asset/_components/dashboard/DayDetail.tsx(88,36): error TS2551 — Property 'category' does not exist on ComputedExpense
```

All 7 errors are in `DayDetail.tsx` and `AssetDashboard.tsx` (the latter fails only because DayDetail/CategoryBreakdown don't yet accept `categories` prop). These are Task 13's domain.

## Self-Review

- Immutable patterns used throughout (no mutations)
- `categoryId ?? undefined` correctly converts null to omit the field from API requests
- Both pages use separate useEffect for categories (non-blocking, errors silently ignored to avoid disrupting the form)
- Chip rendering correctly falls back to empty list if categories haven't loaded yet (no crash)
- No console.log, no hardcoded values

## Fix: 자동선택·로드오류

### Fix 1 — 신규 폼 첫 카테고리 자동선택 (ExpenseForm.tsx)

- `useEffect` import 추가
- 카테고리 목록이 비동기로 도착했을 때 첫 항목을 자동 선택하는 effect 추가:
  - 조건: `initial === null && categoryId === null && categories.length > 0`
  - deps: `[categories, categoryId, initial]` — exhaustive-deps 위반 없음
  - 수정 모드(initial 이 non-null)에서는 동작하지 않음, 사용자가 이미 선택한 경우에도 동작하지 않음

### Fix 2 — 카테고리 로드 실패 표면화

**new/page.tsx**: 기존 에러 UI 없음 → `categoryError` state 추가, 실패 시 에러 박스 + "자산으로" 버튼 표시 (edit 페이지와 동일한 UX 패턴)

**[id]/page.tsx**: 기존 `State` 상태 머신의 `error` 상태 재사용 → `.catch(() => setState({ status: "error", message: "카테고리를 불러오지 못했습니다." }))` 으로 교체

### Lint 결과

```
$ eslint "{app,components,lib,tests}/**/*.{ts,tsx}" "*.{js,ts}" --max-warnings 0
(출력 없음 — 경고·오류 0건, 통과)
```

### TSC 오류 격리 확인 (변경 후)

신규 오류 없음. 기존 Task 13 도메인 오류 7건만 잔존:

```
app/(vault)/asset/_components/dashboard/AssetDashboard.tsx(71,48): TS2322
app/(vault)/asset/_components/dashboard/AssetDashboard.tsx(84,21): TS2322
app/(vault)/asset/_components/dashboard/DayDetail.tsx(4,10): TS2305
app/(vault)/asset/_components/dashboard/DayDetail.tsx(63,66): TS2551
app/(vault)/asset/_components/dashboard/DayDetail.tsx(65,32): TS2551
app/(vault)/asset/_components/dashboard/DayDetail.tsx(76,46): TS2551
app/(vault)/asset/_components/dashboard/DayDetail.tsx(88,36): TS2551
```
