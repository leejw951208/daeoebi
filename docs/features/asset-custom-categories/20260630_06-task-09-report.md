# Task 9 Report: asset-payload — 블롭에서 category 제거 + 레거시 읽기 헬퍼

## Summary

`ExpensePayload`에서 `category: string`을 제거하고(`{ item, amount }`만 유지), `openExpense` 반환값에서도 제거했다. 마이그레이션 전용 함수 `readLegacyCategory(vaultKey, blob): Promise<string | null>`을 추가했다. `IncomePayload` 및 income 관련 코드는 변경 없음.

## TDD RED → GREEN Evidence

### RED (테스트 먼저 수정)

```
$ pnpm --filter web test asset-payload
FAIL node app/(vault)/asset/_lib/asset-payload.spec.ts
  ✕ 지출 본문을 라운드트립한다
    Expected: { item, amount }
    Received: { item, amount, category: "기타" }   ← openExpense 가 category 를 여전히 반환
  ✕ 옛 블롭에서 category 이름을 읽는다(마이그레이션용)
    TypeError: readLegacyCategory is not a function  ← 함수 미존재
  Tests: 2 failed, 2 passed
```

### GREEN (구현 후)

```
$ pnpm --filter web test asset-payload
PASS node app/(vault)/asset/_lib/asset-payload.spec.ts
  ✓ 지출 본문을 라운드트립한다
  ✓ 수입 블롭을 seal→open 라운드트립한다
  ✓ 다른 키로는 복호화되지 않는다
  ✓ 옛 블롭에서 category 이름을 읽는다(마이그레이션용)
Tests: 4 passed, 4 total  Time: 0.195s
```

## Files Changed

- `apps/web/app/(vault)/asset/_lib/asset-payload.ts`
  - `ExpensePayload`: `category: string` 제거 → `{ item: string; amount: number }`
  - `openExpense`: 반환 객체에서 `category` 필드 제거
  - `readLegacyCategory` 함수 추가 (마이그레이션 전용)

- `apps/web/app/(vault)/asset/_lib/asset-payload.spec.ts`
  - 지출 라운드트립 payload에서 `category` 제거
  - "다른 키로는 복호화되지 않는다" 테스트의 `sealExpense` 인수에서 `category` 제거
  - `readLegacyCategory` import 추가
  - `readLegacyCategory` null 반환 테스트 추가

## Self-Review

- Income 경로(`IncomePayload`, `sealIncome`, `openIncome`) 변경 없음 — 기존 테스트 통과 확인.
- `readLegacyCategory`는 `try/catch → null` 패턴으로 복호화 실패 시 안전하게 null 반환.
- Task 15(마이그레이션)에서 이 함수를 사용할 수 있도록 `export` 처리됨.
- 다른 자산 파일은 리팩터링 중이므로 이 모듈만 격리 테스트 — 요구사항과 일치.

## Fix: readLegacyCategory 양성 경로 테스트

### Test Command
```bash
pnpm --filter web test asset-payload
```

### Test Output
```
PASS node app/(vault)/asset/_lib/asset-payload.spec.ts
  asset-payload seal/open
    ✓ 지출 본문을 라운드트립한다 (3 ms)
    ✓ 수입 블롭을 seal→open 라운드트립한다 (1 ms)
    ✓ 다른 키로는 복호화되지 않는다
    ✓ 옛 블롭에서 category 이름을 읽는다(마이그레이션용)
    ✓ 옛 블롭에 category 가 있으면 그 이름을 읽는다 (1 ms)

Test Suites: 1 passed, 1 total
Tests:       5 passed, 5 total
Snapshots:   0 total
Time:        0.192 s, estimated 1 s
```

### Changes
- Import `seal` from `@/lib/vault-crypto`
- Added positive-path test: `readLegacyCategory`에서 legacy blob의 category를 정상적으로 읽는지 검증
- Test crafts legacy-format blob using `seal()` directly with JSON: `{ v: 1, item: "x", amount: 1, category: "식비" }`
- Asserts that `readLegacyCategory(vk, blob)` returns `"식비"`

### Commit
```
077ca24 test(web): readLegacyCategory 양성 경로(category 읽기) 검증 추가
```
