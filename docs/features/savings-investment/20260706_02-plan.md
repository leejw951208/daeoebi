# 저축·투자 관리 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 자산 대시보드에 "저축·투자" 탭을 추가해, 기존 저축/투자 지출을 연동한 순자산·적립 내역과 저축 목표 진행률을 보여준다.

**Architecture:** 저축·투자는 새 도메인이 아니라 **기존 지출(카테고리 `저축`/`투자`)의 파생 뷰**다. 서버는 암호문을 패스스루하고, 클라이언트가 복호화해 집계한다(순수 함수). 저축 목표만 새 `SavingsGoal` 싱글톤으로 암호화 저장한다.

**Tech Stack:** NestJS + Prisma(Postgres) API, Next.js(React) web, Jest 단위, Playwright e2e. 금액은 클라이언트 VK(AES-256-GCM) 암호화.

## Global Constraints

- `apps/api/prisma/migrations/` 기존 파일 수정 금지 — 새 migration만 생성.
- `prisma migrate dev` 는 이 환경에서 대화형이라 막힘 → migration.sql 수동 작성 후 `prisma migrate deploy` + `prisma generate`.
- 금액·본문은 클라이언트 E2E 암호문(iv/ciphertext/authTag base64url) 패스스루. 서버는 복호화하지 않는다.
- 기존 지출·예산·고정지출 로직은 변경하지 않는다(비파괴).
- 저축/투자 식별은 `AssetCategory.name === "저축" | "투자"`.

---

### Task 1: SavingsGoal 모델 + 마이그레이션

**Files:**
- Modify: `apps/api/prisma/schema.prisma`
- Create: `apps/api/prisma/migrations/<TS>_add_savings_goal/migration.sql`

**Interfaces:**
- Produces: Prisma 모델 `SavingsGoal { id, name, iv, ciphertext, authTag, createdAt, updatedAt }`, 생성된 클라이언트의 `prisma.savingsGoal`.

- [ ] **Step 1: 스키마에 모델 추가** (`Income` 모델 아래)

```prisma
// 저축 목표(단건 싱글톤). 금액은 클라이언트 E2E 암호문 블롭({amount}), name 은 평문 라벨.
// 서버는 복호화하지 않는다(income·secret 패스스루와 동일).
model SavingsGoal {
  id         String   @id @default(cuid())
  name       String
  iv         Bytes
  ciphertext Bytes
  authTag    Bytes
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}
```

- [ ] **Step 2: DB 기동(마이그레이션용)**

Run: `docker compose -f docker-compose.dev.yml --env-file apps/api/.env.development up -d db`
Expected: `daeoebi-dev-db Started`

- [ ] **Step 3: migration.sql 수동 작성**

`TS`는 `date +%Y%m%d%H%M%S`. 파일 `apps/api/prisma/migrations/<TS>_add_savings_goal/migration.sql`:

```sql
-- CreateTable
CREATE TABLE "SavingsGoal" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "iv" BYTEA NOT NULL,
    "ciphertext" BYTEA NOT NULL,
    "authTag" BYTEA NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "SavingsGoal_pkey" PRIMARY KEY ("id")
);
```

- [ ] **Step 4: 적용 + 클라이언트 재생성**

Run: `pnpm --filter @daeoebi/api exec prisma migrate deploy && pnpm --filter @daeoebi/api exec prisma generate`
Expected: `All migrations have been successfully applied.` / `Generated Prisma Client`

- [ ] **Step 5: Commit** (사용자 요청 시에만)

```bash
git add apps/api/prisma/schema.prisma apps/api/prisma/migrations
git commit -m "feat(asset): SavingsGoal 모델·마이그레이션 추가"
```

---

### Task 2: SavingsGoal DTO·서비스·컨트롤러 (TDD)

**Files:**
- Create: `apps/api/src/asset/dto/savings-goal.dto.ts`
- Create: `apps/api/src/asset/savings-goal.service.ts`
- Create: `apps/api/src/asset/savings-goal.service.spec.ts`
- Create: `apps/api/src/asset/savings-goal.controller.ts`
- Modify: `apps/api/src/asset/asset.module.ts`

**Interfaces:**
- Consumes: `prisma.savingsGoal`(Task 1), `fromBase64url`/`toBase64url`(`../common/base64url`).
- Produces: `SavingsGoalService.get(): Promise<View|null>`, `upsert(dto): Promise<View>` where `View = { id, name, iv, ciphertext, authTag }`(base64url); route `GET /savings-goal`, `PUT /savings-goal`.

- [ ] **Step 1: DTO 작성** (`SaveSavingsGoalDto`)

```ts
// 저축 목표 저장 DTO. name 평문 + 암호문 블롭({amount}) 3필드.
import { IsBase64url } from "../../common/base64url"
import { IsString, MaxLength, MinLength } from "class-validator"

export class SaveSavingsGoalDto {
    @IsString() @MinLength(1) @MaxLength(40) name!: string
    @IsBase64url() iv!: string
    @IsBase64url() ciphertext!: string
    @IsBase64url() authTag!: string
}
```

- [ ] **Step 2: 서비스 테스트 작성 (실패 확인용)**

`apps/api/src/asset/savings-goal.service.spec.ts`:

```ts
import { SavingsGoalService } from "./savings-goal.service"

function makePrisma() {
    return {
        savingsGoal: {
            findFirst: jest.fn().mockResolvedValue(null),
            create: jest.fn(),
            update: jest.fn(),
        },
    }
}
const dto = { name: "비상금", iv: "aa", ciphertext: "bb", authTag: "cc" }

describe("SavingsGoalService", () => {
    it("get 은 행이 없으면 null", async () => {
        const svc = new SavingsGoalService(makePrisma() as never)
        expect(await svc.get()).toBeNull()
    })

    it("get 은 행이 있으면 view 를 반환한다", async () => {
        const p = makePrisma()
        p.savingsGoal.findFirst.mockResolvedValue({
            id: "1", name: "비상금",
            iv: new Uint8Array([1]), ciphertext: new Uint8Array([2]), authTag: new Uint8Array([3]),
        })
        const svc = new SavingsGoalService(p as never)
        const v = await svc.get()
        expect(v).toMatchObject({ id: "1", name: "비상금" })
        expect(typeof v!.iv).toBe("string")
    })

    it("upsert 는 행이 없으면 create", async () => {
        const p = makePrisma()
        p.savingsGoal.create.mockResolvedValue({ id: "9", name: "비상금", iv: new Uint8Array([1]), ciphertext: new Uint8Array([2]), authTag: new Uint8Array([3]) })
        const svc = new SavingsGoalService(p as never)
        await svc.upsert(dto)
        expect(p.savingsGoal.create).toHaveBeenCalledTimes(1)
        expect(p.savingsGoal.update).not.toHaveBeenCalled()
    })

    it("upsert 는 행이 있으면 update(싱글톤)", async () => {
        const p = makePrisma()
        p.savingsGoal.findFirst.mockResolvedValue({ id: "1" })
        p.savingsGoal.update.mockResolvedValue({ id: "1", name: "비상금", iv: new Uint8Array([1]), ciphertext: new Uint8Array([2]), authTag: new Uint8Array([3]) })
        const svc = new SavingsGoalService(p as never)
        await svc.upsert(dto)
        expect(p.savingsGoal.update).toHaveBeenCalledWith(
            expect.objectContaining({ where: { id: "1" } }),
        )
        expect(p.savingsGoal.create).not.toHaveBeenCalled()
    })
})
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `pnpm --filter @daeoebi/api exec jest savings-goal`
Expected: FAIL (모듈 없음)

- [ ] **Step 4: 서비스 구현** (Income 패스스루 패턴)

```ts
// 저축 목표(SavingsGoal) 싱글톤 CRUD. 본문은 클라이언트 E2E 암호문 패스스루(서버 복호화 없음).
import { Injectable } from "@nestjs/common"
import { PrismaService } from "../prisma/prisma.service"
import { fromBase64url, toBase64url } from "../common/base64url"
import { SaveSavingsGoalDto } from "./dto/savings-goal.dto"

function prismaBytes(v: Uint8Array): Uint8Array<ArrayBuffer> {
    return new Uint8Array(v)
}
interface Row { id: string; name: string; iv: Uint8Array; ciphertext: Uint8Array; authTag: Uint8Array }
function toView(row: Row) {
    return {
        id: row.id, name: row.name,
        iv: toBase64url(row.iv), ciphertext: toBase64url(row.ciphertext), authTag: toBase64url(row.authTag),
    }
}

@Injectable()
export class SavingsGoalService {
    constructor(private readonly prisma: PrismaService) {}

    async get() {
        const row = await this.prisma.savingsGoal.findFirst({ orderBy: { createdAt: "asc" } })
        return row ? toView(row) : null
    }

    async upsert(dto: SaveSavingsGoalDto) {
        const existing = await this.prisma.savingsGoal.findFirst({ orderBy: { createdAt: "asc" }, select: { id: true } })
        const data = {
            name: dto.name,
            iv: prismaBytes(fromBase64url(dto.iv)),
            ciphertext: prismaBytes(fromBase64url(dto.ciphertext)),
            authTag: prismaBytes(fromBase64url(dto.authTag)),
        }
        const row = existing
            ? await this.prisma.savingsGoal.update({ where: { id: existing.id }, data })
            : await this.prisma.savingsGoal.create({ data })
        return toView(row)
    }
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `pnpm --filter @daeoebi/api exec jest savings-goal`
Expected: PASS (4 tests)

- [ ] **Step 6: 컨트롤러 작성**

```ts
// 저축 목표 엔드포인트. 전역 세션 가드 + CsrfMiddleware. 본문 암호문 패스스루.
import { Body, Controller, Get, Put } from "@nestjs/common"
import { SavingsGoalService } from "./savings-goal.service"
import { SaveSavingsGoalDto } from "./dto/savings-goal.dto"

@Controller("savings-goal")
export class SavingsGoalController {
    constructor(private readonly service: SavingsGoalService) {}

    @Get()
    get() {
        return this.service.get()
    }

    @Put()
    save(@Body() dto: SaveSavingsGoalDto) {
        return this.service.upsert(dto)
    }
}
```

- [ ] **Step 7: 모듈 등록** (`asset.module.ts` — controllers·providers 배열과 CsrfMiddleware.forRoutes 에 `SavingsGoalController`/`SavingsGoalService` 추가, import 문 추가)

- [ ] **Step 8: 정적 검증**

Run: `pnpm --filter @daeoebi/api run typecheck && pnpm --filter @daeoebi/api exec jest`
Expected: typecheck Done, 전체 PASS

- [ ] **Step 9: Commit** (사용자 요청 시에만)

```bash
git add apps/api/src/asset
git commit -m "feat(asset): 저축 목표 API(SavingsGoal) 추가"
```

---

### Task 3: Expense.listContributions (TDD)

**Files:**
- Modify: `apps/api/src/asset/expense.service.ts`
- Modify: `apps/api/src/asset/expense.service.spec.ts`
- Modify: `apps/api/src/asset/expense.controller.ts`

**Interfaces:**
- Produces: `ExpenseService.listContributions(categoryIds: string[]): Promise<ExpenseView[]>`; route `GET /expenses/contributions?categoryIds=a,b`.

- [ ] **Step 1: 테스트 추가** (`expense.service.spec.ts` — 기존 makePrisma 에 `expense.findMany` 가 있으면 재사용)

```ts
it("listContributions 는 categoryIds 로 필터하고 removed 를 제외한다", async () => {
    const prisma = makePrisma()
    prisma.expense.findMany.mockResolvedValue([])
    const svc = new ExpenseService(prisma as never)
    await svc.listContributions(["s", "i"])
    expect(prisma.expense.findMany).toHaveBeenCalledWith({
        where: { removed: false, categoryId: { in: ["s", "i"] } },
        orderBy: { date: "desc" },
    })
})

it("listContributions 는 빈 ids 면 조회 없이 빈 배열", async () => {
    const prisma = makePrisma()
    const svc = new ExpenseService(prisma as never)
    expect(await svc.listContributions([])).toEqual([])
    expect(prisma.expense.findMany).not.toHaveBeenCalled()
})
```

> 기존 `expense.service.spec.ts` 의 `makePrisma` 에 `expense.findMany: jest.fn()` 이 없으면 추가한다.

- [ ] **Step 2: 실패 확인**

Run: `pnpm --filter @daeoebi/api exec jest expense.service`
Expected: FAIL

- [ ] **Step 3: 서비스 구현** (`expense.service.ts` — `listByMonth` 아래 추가)

```ts
// 저축·투자 탭용: 전 기간(모든 달) 비삭제 지출을 카테고리로 필터해 반환(클라가 복호화·집계).
async listContributions(categoryIds: string[]) {
    if (categoryIds.length === 0) return []
    const rows = await this.prisma.expense.findMany({
        where: { removed: false, categoryId: { in: categoryIds } },
        orderBy: { date: "desc" },
    })
    return rows.map(toView)
}
```

- [ ] **Step 4: 통과 확인**

Run: `pnpm --filter @daeoebi/api exec jest expense.service`
Expected: PASS

- [ ] **Step 5: 컨트롤러 라우트 추가** (`expense.controller.ts` — **반드시 `@Get(":id")` 위**에 배치, 아니면 `contributions` 가 id로 매칭됨)

```ts
@Get("contributions")
contributions(@Query("categoryIds") categoryIds?: string) {
    const ids = (categoryIds ?? "").split(",").map((s) => s.trim()).filter(Boolean)
    return this.service.listContributions(ids)
}
```

- [ ] **Step 6: 정적 검증 + Commit(요청 시)**

Run: `pnpm --filter @daeoebi/api run typecheck && pnpm --filter @daeoebi/api exec jest`
```bash
git add apps/api/src/asset
git commit -m "feat(asset): 저축·투자 전기간 적립 조회(listContributions) 추가"
```

---

### Task 4: asset-compute 집계 함수 (TDD)

**Files:**
- Modify: `apps/web/app/(vault)/asset/_lib/asset-compute.ts`
- Modify: `apps/web/app/(vault)/asset/_lib/asset-compute.spec.ts`

**Interfaces:**
- Produces:
  - `interface SavingsSummary { savedTotal: number; investTotal: number; netWorth: number }`
  - `savingsSummary(contribs: readonly {categoryId: string|null; amount: number}[], categories: readonly {id: string; name: string}[]): SavingsSummary`
  - `goalProgress(savedTotal: number, goalAmount: number): number`

- [ ] **Step 1: 테스트 추가** (`asset-compute.spec.ts`)

```ts
describe("savingsSummary", () => {
    const cats = [
        { id: "s", name: "저축" },
        { id: "i", name: "투자" },
        { id: "f", name: "식비" },
    ]
    it("저축/투자를 분리 합산하고 순자산을 낸다", () => {
        const r = savingsSummary(
            [
                { categoryId: "s", amount: 100000 },
                { categoryId: "i", amount: 50000 },
                { categoryId: "s", amount: 30000 },
                { categoryId: "f", amount: 9000 },
                { categoryId: null, amount: 1000 },
            ],
            cats,
        )
        expect(r).toEqual({ savedTotal: 130000, investTotal: 50000, netWorth: 180000 })
    })
})

describe("goalProgress", () => {
    it("진행률은 정수·0~100 클램프", () => {
        expect(goalProgress(50000, 100000)).toBe(50)
        expect(goalProgress(150000, 100000)).toBe(100)
        expect(goalProgress(1000, 0)).toBe(0)
    })
})
```

> import 문에 `savingsSummary`, `goalProgress` 추가.

- [ ] **Step 2: 실패 확인**

Run: `pnpm --filter @daeoebi/web exec jest asset-compute`
Expected: FAIL

- [ ] **Step 3: 구현** (`asset-compute.ts` 말미)

```ts
export interface SavingsSummary {
    savedTotal: number
    investTotal: number
    netWorth: number
}

// 저축/투자 카테고리 지출을 이름 기준으로 분리 합산한다. netWorth = 저축+투자.
export function savingsSummary(
    contribs: readonly { categoryId: string | null; amount: number }[],
    categories: readonly { id: string; name: string }[],
): SavingsSummary {
    const nameById = new Map(categories.map((c) => [c.id, c.name]))
    let savedTotal = 0
    let investTotal = 0
    for (const c of contribs) {
        const name = c.categoryId ? nameById.get(c.categoryId) : undefined
        if (name === "저축") savedTotal += c.amount
        else if (name === "투자") investTotal += c.amount
    }
    return { savedTotal, investTotal, netWorth: savedTotal + investTotal }
}

// 저축 목표 진행률(%). 목표 0 이하면 0. 정수·0~100 클램프.
export function goalProgress(savedTotal: number, goalAmount: number): number {
    if (goalAmount <= 0) return 0
    return Math.min(100, Math.round((savedTotal / goalAmount) * 100))
}
```

- [ ] **Step 4: 통과 확인 + Commit(요청 시)**

Run: `pnpm --filter @daeoebi/web exec jest asset-compute`
```bash
git add "apps/web/app/(vault)/asset/_lib/asset-compute.ts" "apps/web/app/(vault)/asset/_lib/asset-compute.spec.ts"
git commit -m "feat(asset): 저축·투자 집계 함수(savingsSummary·goalProgress)"
```

---

### Task 5: asset-payload 목표 봉인 + vault-client

**Files:**
- Modify: `apps/web/app/(vault)/asset/_lib/asset-payload.ts`
- Modify: `apps/web/lib/vault-client.ts`

**Interfaces:**
- Consumes: `seal`/`open`(`@/lib/vault-crypto`), `ExpenseView`·`SealedBlobDto`(vault-client).
- Produces: `sealGoal(vk, {amount})`, `openGoal(vk, blob)`; `SavingsGoalView`, `getSavingsGoal()`, `saveSavingsGoal(name, blob)`, `listContributions(ids)`.

- [ ] **Step 1: asset-payload 에 목표 seal/open 추가**

```ts
export interface GoalPayload {
    amount: number
}
export async function sealGoal(vaultKey: CryptoKey, payload: GoalPayload): Promise<SealedBlob> {
    return seal(vaultKey, JSON.stringify({ v: PAYLOAD_VERSION, ...payload }))
}
export async function openGoal(vaultKey: CryptoKey, blob: SealedBlob): Promise<GoalPayload> {
    const parsed = JSON.parse(await open(vaultKey, blob)) as { amount?: unknown }
    return { amount: typeof parsed.amount === "number" ? parsed.amount : 0 }
}
```

- [ ] **Step 2: vault-client 함수 추가** (자산 카테고리 섹션 근처)

```ts
export interface SavingsGoalView extends SealedBlobDto {
    id: string
    name: string
}

export async function getSavingsGoal(): Promise<SavingsGoalView | null> {
    const { data } = await vaultClient.get<SavingsGoalView | null>("/savings-goal")
    return data ?? null
}

export async function saveSavingsGoal(
    name: string,
    blob: SealedBlobDto,
): Promise<SavingsGoalView> {
    const { data } = await vaultClient.put<SavingsGoalView>("/savings-goal", {
        name,
        ...blob,
    })
    return data
}

// 저축·투자 카테고리(저축/투자)의 전 기간 적립 지출.
export async function listContributions(
    categoryIds: string[],
): Promise<ExpenseView[]> {
    const { data } = await vaultClient.get<ExpenseView[]>(
        "/expenses/contributions",
        { params: { categoryIds: categoryIds.join(",") } },
    )
    return data
}
```

- [ ] **Step 3: 정적 검증 + Commit(요청 시)**

Run: `pnpm --filter @daeoebi/web run typecheck`
```bash
git add "apps/web/app/(vault)/asset/_lib/asset-payload.ts" apps/web/lib/vault-client.ts
git commit -m "feat(asset): 저축 목표 봉인 헬퍼·vault-client 함수 추가"
```

---

### Task 6: 저축·투자 탭 컴포넌트

**Files:**
- Create: `apps/web/app/(vault)/asset/_components/dashboard/SavingsTab.tsx`

**Interfaces:**
- Consumes: `SavingsSummary`·`goalProgress`(Task 4), `AssetCategory`·`ExpenseView`(vault-client), 복호화된 값(부모가 전달).
- Produces: `<SavingsTab summary savedMonth investMonth goalName goalAmount contributions onEditGoal />` — Props 타입은 파일 상단에 정의.

- [ ] **Step 1: 컴포넌트 작성** (디자인 저축·투자 탭 구조: 순자산 hero → 저축/투자 카드 → 저축 목표 → 이번 달 적립 내역. 기존 `asset-card`·`entry-card` 스타일과 `formatWon` 재사용)

```tsx
"use client"
// 저축·투자 탭. 저축/투자 순자산·이번 달 적립·저축 목표 진행률·적립 내역을 보여준다.
// 데이터(누적/월별/목표)는 부모(asset/page)가 복호화해 props 로 넘긴다.
import type { SavingsSummary } from "../../_lib/asset-compute"
import { goalProgress } from "../../_lib/asset-compute"
import { formatWon } from "../../_lib/asset-categories"

interface Contribution {
    id: string
    item: string
    amount: number
    categoryName: string
    date: string
}
interface SavingsTabProps {
    summary: SavingsSummary
    savedMonth: number
    investMonth: number
    goalName: string | null
    goalAmount: number
    contributions: Contribution[]
    onEditGoal: () => void
}

export function SavingsTab({
    summary,
    savedMonth,
    investMonth,
    goalName,
    goalAmount,
    contributions,
    onEditGoal,
}: SavingsTabProps) {
    const pct = goalProgress(summary.savedTotal, goalAmount)
    const monthContrib = savedMonth + investMonth
    return (
        <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
            {/* 순자산 */}
            <div className="asset-card">
                <div className="asset-card-label">저축·투자 순자산</div>
                <div style={{ fontSize: 32, fontWeight: 800 }}>
                    {formatWon(summary.netWorth)}
                </div>
                <div style={{ fontSize: 12.5, color: "var(--color-text-muted)", marginTop: 8 }}>
                    이번 달 {formatWon(monthContrib)} 적립 · {contributions.filter((c) => c.date.slice(0, 7) === /* 현재월은 부모가 필터해 전달 */ c.date.slice(0, 7)).length}건
                </div>
            </div>

            {/* 저축 / 투자 카드 */}
            <div style={{ display: "flex", gap: 12 }}>
                <div className="asset-card" style={{ flex: 1 }}>
                    <div className="asset-card-label">저축</div>
                    <div style={{ fontSize: 18, fontWeight: 800 }}>{formatWon(summary.savedTotal)}</div>
                    <div style={{ fontSize: 11, color: "#20a4a4", fontWeight: 700, marginTop: 5 }}>이번 달 {formatWon(savedMonth)}</div>
                </div>
                <div className="asset-card" style={{ flex: 1 }}>
                    <div className="asset-card-label">투자</div>
                    <div style={{ fontSize: 18, fontWeight: 800 }}>{formatWon(summary.investTotal)}</div>
                    <div style={{ fontSize: 11, color: "#7b61ff", fontWeight: 700, marginTop: 5 }}>이번 달 {formatWon(investMonth)}</div>
                </div>
            </div>

            {/* 저축 목표 */}
            <button type="button" className="asset-card" onClick={onEditGoal} style={{ textAlign: "left", cursor: "pointer" }}>
                <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 12 }}>
                    <span style={{ fontWeight: 800, fontSize: 13 }}>
                        저축 목표{goalName ? ` · ${goalName}` : ""}
                    </span>
                    <span style={{ fontWeight: 800, fontSize: 12.5, color: "#20a4a4" }}>
                        {goalAmount > 0 ? `${pct}%` : "설정하기"}
                    </span>
                </div>
                <div style={{ height: 8, borderRadius: 999, background: "var(--color-surface-muted, #f1f1f1)", overflow: "hidden" }}>
                    <div style={{ width: `${pct}%`, height: "100%", background: "#20a4a4" }} />
                </div>
                <div style={{ display: "flex", justifyContent: "space-between", fontSize: 12, color: "var(--color-text-muted)", marginTop: 9 }}>
                    <span>{formatWon(summary.savedTotal)}</span>
                    <span>목표 {goalAmount > 0 ? formatWon(goalAmount) : "-"}</span>
                </div>
            </button>

            {/* 이번 달 적립 내역 */}
            <div className="asset-card">
                <div style={{ fontWeight: 800, fontSize: 13, marginBottom: 12 }}>이번 달 적립 내역</div>
                {contributions.length === 0 ? (
                    <div style={{ textAlign: "center", padding: "18px 0", fontSize: 13, color: "var(--color-text-muted)", fontWeight: 600 }}>
                        이번 달 적립이 없어요
                    </div>
                ) : (
                    <div style={{ display: "flex", flexDirection: "column", gap: 9 }}>
                        {contributions.map((c) => (
                            <div key={c.id} className="entry-card" style={{ justifyContent: "space-between" }}>
                                <span style={{ fontSize: 15, fontWeight: 700 }}>{c.item}</span>
                                <span style={{ fontSize: 12, color: "var(--color-text-muted)" }}>{c.categoryName}</span>
                                <span style={{ fontSize: 15, fontWeight: 800 }}>{formatWon(c.amount)}</span>
                            </div>
                        ))}
                    </div>
                )}
            </div>
        </div>
    )
}
```

> 순자산의 "N건"은 부모가 넘긴 이번 달 `contributions` 길이로 계산한다(위 인라인 필터 대신 `contributions.length` 사용 — 부모가 이번 달치만 넘긴다). 구현 시 `{contributions.length}건`으로 단순화.

- [ ] **Step 2: 정적 검증**

Run: `pnpm --filter @daeoebi/web run typecheck`
Expected: Done (SavingsTab 미사용 경고는 다음 태스크에서 배선)

- [ ] **Step 3: Commit(요청 시)**

```bash
git add "apps/web/app/(vault)/asset/_components/dashboard/SavingsTab.tsx"
git commit -m "feat(asset): 저축·투자 탭 컴포넌트"
```

---

### Task 7: 저축 목표 설정 시트

**Files:**
- Create: `apps/web/app/(vault)/asset/_components/SavingsGoalSheet.tsx`

**Interfaces:**
- Consumes: `Button`, `useVault`, `sealGoal`(Task 5), `saveSavingsGoal`(Task 5), `formatAmount`(asset-categories).
- Produces: `<SavingsGoalSheet initialName initialAmount onSaved onClose />`.

- [ ] **Step 1: 컴포넌트 작성** (`BudgetSheet.tsx` 패턴 복제 — 이름 입력 + ₩ 금액 입력 + 취소/저장, VK 암호화, 실패 문구 일치)

```tsx
"use client"
// 저축 목표 설정 바텀시트. 이름 + 금액(₩). 금액은 VK 로 암호화 저장(sealGoal → saveSavingsGoal).
import { useState } from "react"
import { Button } from "@/components/Button"
import { useVault } from "../../_lib/vault-context"
import { saveSavingsGoal } from "@/lib/vault-client"
import { sealGoal } from "../_lib/asset-payload"
import { formatAmount } from "../_lib/asset-categories"

const MAX_AMOUNT_DIGITS = 12

interface Props {
    initialName: string
    initialAmount: number
    onSaved: () => void | Promise<void>
    onClose: () => void
}

export function SavingsGoalSheet({ initialName, initialAmount, onSaved, onClose }: Props) {
    const { vaultKey, resetIdle } = useVault()
    const [name, setName] = useState(initialName)
    const [amount, setAmount] = useState(initialAmount > 0 ? String(initialAmount) : "")
    const [saving, setSaving] = useState(false)
    const [saveFailed, setSaveFailed] = useState(false)
    const value = Number(amount || "0")

    async function save() {
        if (saving || !name.trim() || value <= 0) return
        setSaving(true)
        setSaveFailed(false)
        try {
            const blob = await sealGoal(vaultKey, { amount: value })
            await saveSavingsGoal(name.trim(), blob)
            await onSaved()
            onClose()
        } catch {
            setSaveFailed(true)
        } finally {
            setSaving(false)
        }
    }

    return (
        <div className="dialog-backdrop" role="dialog" aria-modal="true" aria-label="저축 목표 설정"
             onClick={(e) => { if (e.target === e.currentTarget && !saving) onClose() }}>
            <div className="sheet">
                <div className="sheet-grip" aria-hidden="true" />
                <div style={{ fontSize: 18, fontWeight: 800, marginBottom: 4 }}>저축 목표</div>
                <p className="muted" style={{ fontSize: 13, marginBottom: 16 }}>목표 이름과 금액을 정해두세요.</p>
                <input className="field-control" placeholder="예: 비상금" value={name} maxLength={40}
                       aria-label="저축 목표 이름"
                       onChange={(e) => { resetIdle(); setName(e.target.value) }}
                       style={{ marginBottom: 12 }} />
                <div className="income-input">
                    <span aria-hidden="true">₩</span>
                    <input inputMode="numeric" value={amount ? formatAmount(value) : ""} placeholder="0"
                           aria-label="저축 목표 금액"
                           onChange={(e) => { resetIdle(); setAmount(e.target.value.replace(/[^\d]/g, "").slice(0, MAX_AMOUNT_DIGITS)) }} />
                </div>
                {saveFailed && (
                    <div role="alert" className="error-box" style={{ marginTop: 12 }}>저장하지 못했어요. 다시 시도해 주세요.</div>
                )}
                <div style={{ display: "flex", gap: 9, marginTop: 16 }}>
                    <Button variant="secondary" style={{ flex: 1 }} onClick={onClose} disabled={saving}>취소</Button>
                    <Button variant="primary" style={{ flex: 2 }} onClick={() => { resetIdle(); void save() }}
                            loading={saving} disabled={!name.trim() || value <= 0}>저장</Button>
                </div>
            </div>
        </div>
    )
}
```

- [ ] **Step 2: 정적 검증 + Commit(요청 시)**

Run: `pnpm --filter @daeoebi/web run typecheck`
```bash
git add "apps/web/app/(vault)/asset/_components/SavingsGoalSheet.tsx"
git commit -m "feat(asset): 저축 목표 설정 시트"
```

---

### Task 8: 대시보드 세그먼트 + page 배선

**Files:**
- Modify: `apps/web/app/(vault)/asset/_components/dashboard/AssetDashboard.tsx`
- Modify: `apps/web/app/(vault)/asset/page.tsx`

**Interfaces:**
- Consumes: `SavingsTab`(Task 6), `SavingsGoalSheet`(Task 7), `savingsSummary`(Task 4), `getSavingsGoal`·`listContributions`(Task 5), `openGoal`·`openExpense`(asset-payload).

- [ ] **Step 1: `AssetDashboard` 에 세그먼트 추가** — `assetTab` prop(`"budget" | "savings"`)과 `onTab` 콜백을 받아 상단에 세그먼트 버튼 2개(`이번 달` / `저축·투자`)를 렌더. `budget` 이면 기존 본문, `savings` 면 `<SavingsTab .../>`. (세그먼트 스타일은 간단한 pill 토글; 기존 클래스 재사용)

- [ ] **Step 2: `asset/page.tsx` 배선**
  - state: `assetTab`, `savingsLoaded`, `contribAll`(전기간 복호화 `{categoryId, amount, date, item}`), `goal`(`{name, amount} | null`), `showGoalSheet`.
  - 저축·투자 카테고리 id 계산: `categories.filter(c => c.name === "저축" || c.name === "투자").map(c => c.id)`.
  - 탭이 `savings` 로 처음 바뀌면 지연 로드: `listContributions(ids)` → 각 블롭 `openExpense(vaultKey, blob)` 복호화(실패 스킵) → `contribAll`; `getSavingsGoal()` → `openGoal` 복호화 → `goal`.
  - `savingsSummary(contribAll, categories)` 로 누적, 현재 `month` 로 이번 달 저축/투자·적립 리스트 산출해 `SavingsTab` 에 전달.
  - `onEditGoal` → `showGoalSheet=true`; `SavingsGoalSheet` 의 `onSaved` → goal 재로드.

- [ ] **Step 3: 정적 검증 + 단위 테스트**

Run: `pnpm -r run typecheck && pnpm -r run test`
Expected: 전부 PASS

- [ ] **Step 4: Commit(요청 시)**

```bash
git add "apps/web/app/(vault)/asset"
git commit -m "feat(asset): 대시보드 저축·투자 세그먼트·목표 시트 배선"
```

---

### Task 9: e2e

**Files:**
- Create: `apps/web/tests/e2e/savings.spec.ts`

- [ ] **Step 1: 스펙 작성** (dev-unlock → 자산 → "저축·투자" 세그먼트 클릭 → "저축·투자 순자산" 노출 → 저축 목표 "설정하기" → 이름·금액 저장 → 진행률/목표 반영). `category-crud.spec.ts` 의 `enterVaultAt`·뷰포트 패턴 재사용.

- [ ] **Step 2: 실행**

Run: `pnpm --filter @daeoebi/web run test:e2e savings` (dev 서버 필요: `make dev-up`)
Expected: PASS

- [ ] **Step 3: Commit(요청 시)** `test(asset): 저축·투자 e2e`

---

### Task 10: QA 시나리오

**Files:**
- Modify: `docs/qa/qa-scenarios.md`

- [ ] **Step 1:** 커버리지 표에 `가계부-저축투자 | SAVINGS | 4` 추가, 아래 케이스 추가:
  - `SAVINGS-001` 세그먼트 전환 — "저축·투자" 탭 → 순자산·저축/투자 카드 노출
  - `SAVINGS-002` 목표 설정 — 이름+금액 저장 → 진행률·목표액 반영
  - `SAVINGS-003` 적립 집계 — 저축/투자 지출 추가 후 순자산·이번 달 적립 증가
  - `SAVINGS-004` 비파괴 — 저축/투자 지출이 "이번 달" 탭 지출·남은 예산에도 그대로 반영
- [ ] **Step 2: Commit(요청 시)** `docs: QA 저축·투자(SAVINGS) 시나리오 추가`

---

## Self-Review

- **Spec 커버리지**: 데이터모델(T1) · 백엔드 SavingsGoal(T2)·listContributions(T3) · 계산(T4) · 클라이언트/암호화(T5) · UI 탭(T6)·목표시트(T7)·배선(T8) · 테스트(T2~T4 단위, T9 e2e, T10 QA). 스펙 9개 섹션 모두 태스크 대응.
- **Placeholder**: 없음(코드·명령·기대결과 명시). UI 배선(T8)은 단계 설명 + 소비 인터페이스 명시로 결정 잠금.
- **타입 일관성**: `savingsSummary`/`goalProgress`/`SavingsSummary`/`SavingsGoalView`/`sealGoal`/`openGoal`/`listContributions` 이름·시그니처가 태스크 간 일치.
- **주의**: `@Get("contributions")` 는 `@Get(":id")` 위(라우트 우선). 순자산 "N건"은 부모가 넘긴 이번 달 contributions 길이 사용.
