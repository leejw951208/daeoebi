# Task 2: 데모 카테고리 관리 컴포넌트 — 완료

## 작업 개요
데모 페이지용 카테고리 관리 컴포넌트(로컬 상태)를 작성했습니다.

## 변경 사항

### 신규 파일: `apps/web/app/demo/DemoCategoryManager.tsx` (241줄)
- **목적**: 실제 CategoryManager의 축약 버전으로, 로컬 상태만 사용 (vault-client/useVault 미사용)
- **인터페이스**:
  - Props: `categories: AssetCategory[]`, `onChange(next: AssetCategory[])`, `onClose()`
  - 모든 상태 변경은 props 콜백으로 전달
- **기능**:
  - **추가**: name/color 입력 후 "+ 추가" 클릭
    - 자동 ID 생성: `demo-cat-${demoCatSeq}`
    - 타임스탬프 고정: `2026-06-30T00:00:00.000Z`
    - 유효성: name 필수, color 유효한 HEX
  - **수정**: 행의 "수정" 클릭 시 인라인 편집
    - "저장"/"취소" 버튼으로 제어
  - **삭제**: ConfirmDialog를 통한 확인 후 제거
- **재사용 컴포넌트**:
  - `Button` from `@/components/Button`
  - `ConfirmDialog` from `@/components/ConfirmDialog`
  - `CategoryColorInput` from `../(vault)/asset/_components/CategoryColorInput`
  - `isValidHexColor` from `../(vault)/asset/_lib/asset-categories`
- **마크업**: 실제 CategoryManager 스타일·클래스 매칭
- **접근성**: role, aria-label, aria-hidden 포함

## 검증 결과

### TypeScript 타입 체크
```bash
$ pnpm --filter web exec tsc --noEmit
```
✓ **PASS** — 타입 에러 없음

### ESLint 린트
```bash
$ pnpm --filter web lint
$ eslint "{app,components,lib,tests}/**/*.{ts,tsx}" "*.{js,ts}" --max-warnings 0
```
✓ **PASS** — 모든 규칙 통과 (린터가 JSX 속성 포맷팅 자동 정정)

## 자체 검토

### 코드 품질
- ✓ `any` 타입 미사용
- ✓ `console.*` 미사용
- ✓ `React.FC` 미사용 (함수 컴포넌트 직접 export)
- ✓ Props는 명명된 인터페이스(`DemoCategoryManagerProps`)로 정의
- ✓ 불변성: spread operator로 새 배열 생성

### 기능 정합성
- ✓ 모든 임포트 경로 정확 (type-only AssetCategory)
- ✓ 로컬 상태 격리: vault-client/useVault 미사용
- ✓ 콜백 기반 상태 관리: onChange/onClose 활용
- ✓ 유효성 검사: isValidHexColor 활용

### 레이아웃·스타일
- ✓ CategoryManager와 동일한 UX 마크업
- ✓ ConfirmDialog 통합 (삭제 확인)
- ✓ 인라인 스타일로 레이아웃 제어

## 커밋 정보
- **커밋 SHA**: `0a7c0d5`
- **커밋 메시지**: `feat(web): 데모 카테고리 관리 컴포넌트(로컬 상태)`
- **파일 변경**:
  - `apps/web/app/demo/DemoCategoryManager.tsx` — 신규 생성 (241줄)
