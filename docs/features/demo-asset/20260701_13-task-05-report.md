## Task 5 Report: 데모 페이지 탭 토글 통합

### What Changed

**File modified:** `apps/web/app/demo/page.tsx`

**Additions:**
- `import { type CSSProperties, useMemo, useState } from "react"` — added `CSSProperties` type import
- `import { Button } from "@/components/Button"` — for optional button swap
- `import { DemoAssetScreen } from "./DemoAssetScreen"` — Task 4 asset screen
- `const [tab, setTab] = useState<"secret" | "asset">("secret")` — top-level tab state in `DemoPage`
- Early return guard: `if (tab === "asset") return <section><DemoBanner /><DemoTabs tab={tab} onTab={setTab} /><DemoAssetScreen /></section>`
- `<DemoTabs tab={tab} onTab={setTab} />` placed immediately after `<DemoBanner />` in all four return branches (new/edit form, detail-not-found, detail-found, list)
- `DemoTabs` local component added at file bottom (uses `CSSProperties` base style + two inline `<button>` elements)

### Where DemoTabs Was Placed

Appears in every branch of `DemoPage`'s render:
1. Asset tab early return (top guard)
2. new/edit form section
3. detail "not found" fallback section
4. detail "found" section (after `<DemoBanner />`, before `sticky-header`)
5. list section (after `<DemoBanner />`, before `sticky-header`)

Switching to 가계부 and back preserves the password `view` state because `tab` and `view` are independent state variables.

### Optional Button Swap

Done. Swapped 3 buttons in `page.tsx`:
- "데모 목록으로" (`className="btn"`) → `<Button>`
- "수정" (`className="btn secondary"`) → `<Button variant="secondary">`
- "삭제" (`className="btn danger"`) → `<Button variant="danger">`

The `fab` button and `btn-text` nav buttons were left as-is (no matching `Button` variant).

### Verification Output

- `tsc --noEmit`: PASS (no output)
- `lint`: PASS (no warnings)
- `build`: PASS — `/demo` route 8.5 kB, all routes green
- `test`: PASS — 12 suites, 58 tests

### Self-Review

- No `any`, no `React.FC`, no `console.*`
- Demo isolation preserved — no vault-client, no `useVault` usage
- `view` state not reset on tab switch
- `DemoTabs` component is a clean local function below `nowIso()`
- File is 514 lines, within the 800-line limit
