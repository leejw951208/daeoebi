## Task 3 Report: 데모 지출 추가 폼(로컬 상태)

### What Changed
Created `apps/web/app/demo/DemoExpenseForm.tsx` — demo-only expense-add form with local state (mirrors real ExpenseForm but isolated from vault-client).

### Component Summary
- **File**: `apps/web/app/demo/DemoExpenseForm.tsx` (186 lines)
- **Exports**: `DemoExpenseForm({ categories, onSave, onCancel })`
- **Local state**: amount, item, categoryId, date (with DEMO_MONTH-15 default)
- **Behavior**: 
  - Increments demo sequence on each save
  - Returns `ComputedExpense` with demo ID (`demo-exp-${seq}`)
  - Numeric input validates digits-only, max 12 digits
  - Category chips with visual feedback
  - Date picker with demo month default

### Verification Results

| 단계 | 결과 |
|------|------|
| `tsc --noEmit` | PASS (no errors) |
| `pnpm --filter web lint` | PASS (ESLint completed) |

Note: Linter auto-formatted line 91-94 (amount onChange callback) for line length; no logic changes.

### Commit
```
1a8c401 feat(web): 데모 지출 추가 폼(로컬 상태)
```

### Self-Review Checklist
- [x] Exact code from brief (linter formatting only)
- [x] Correct imports (AssetCategory, ComputedExpense, Button, DEMO_MONTH)
- [x] No vault-client runtime usage
- [x] Local state only (no useVault)
- [x] No TypeScript errors
- [x] No lint violations
- [x] No `any`, `console.*`, `React.FC`
- [x] Component exports function (not default)
- [x] Appropriate error handling (amountNum <= 0 check in save)
