# Task 7 Report: e2e 로딩 표시 검증 + 최종 그린

## 추가된 단언 (Test F)

**파일**: `apps/web/tests/e2e/asset.spec.ts`  
**테스트명**: `F. loading — 추가 버튼이 진행 중에 aria-busy 됨`

**접근 방식**: route-delay  
`page.route("**/asset-categories")` 로 POST 요청을 300 ms 지연시켜 `Button`의 `loading={saving}` 상태(= `aria-busy="true"` + `disabled`)를 관측 가능하게 만든 뒤, `toHaveAttribute("aria-busy", "true", { timeout: 500 })` 로 단언. 요청 완료 후 카테고리 생성 확인 → 삭제로 정리.

## e2e 결과 (2회 실행)

**Run 1**: 6 passed (9.2 s)  
**Run 2**: 6 passed (8.1 s)  
→ 안정적. 플레이크 없음.

## 정적 검증

| 검사 | 결과 |
|------|------|
| `tsc --noEmit` | PASS (출력 없음) |
| `lint --max-warnings 0` | PASS |
| `jest` (unit) | PASS — 11 suites, 53 tests |
| `next build` | PASS |

## 플레이크 자가 평가

route-delay 방식으로 loading 상태가 최소 300 ms 유지되어 `aria-busy` 단언이 항상 성립함. 2회 연속 안정 확인. 단언 timeout(500 ms)은 delay(300 ms)보다 크므로 여유 있음.

## 커밋

`d9d7195 test(web): 저장 액션 로딩 표시 e2e 보강`
